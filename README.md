# ntpdated

**ntpdated** — это легковесный фоновый демон для систем с **sysvinit**, предназначенный для однократной синхронизации времени при загрузке.

В отличие от стандартных клиентов, `ntpdated` запускается в фоне и "терпеливо" ожидает появления интернет-соединения (например, через LTE/5G модем). Как только время успешно синхронизировано и записано в BIOS, демон автоматически завершает работу, освобождая ресурсы системы.

## ✨ Особенности
- **Zero Configuration**: Работает "из коробки" через публичные NTP-серверы.
- **Non-blocking**: Не задерживает загрузку операционной системы.
- **DNS-independent**: Использует IP-адреса серверов Cloudflare/Google для работы до старта DNS.
- **Auto-exit**: Завершает процесс сразу после успешной синхронизации.
- **Hardware Sync**: Автоматически обновляет системные часы BIOS (RTC).

## 🛠 Требования
- Linux с системой инициализации **sysvinit**.
- Установленные утилиты: `ntpdate`, `hwclock`, `bash`.

## 🚀 Установка

Вы можете установить проект одной командой:

```bash
git clone github.com
cd ntpdated
sudo chmod +x install.sh
sudo ./install.sh
